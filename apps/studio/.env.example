# Studio应用环境变量配置示例

# 应用标题
VITE_APP_TITLE=SKER Studio

# Gateway API网关地址 (WebSocket + HTTP API)
# 开发环境: http://localhost:8000
# Docker环境: http://gateway:8000 (自动检测)
VITE_GATEWAY_URL=http://localhost:8000

# Store数据存储服务地址
# 开发环境: http://localhost:3001
# Docker环境: http://store:3001
VITE_STORE_URL=http://localhost:3001

# AI Engine服务地址(可选,通常通过Gateway代理访问)
VITE_AI_API_URL=http://localhost:8001/api/ai

# WebSocket服务地址 (连接Gateway的Socket.IO)
# 开发环境: http://localhost:8000
# Docker环境: http://gateway:8000 (自动检测)
# 注意: Socket.IO会自动升级为WebSocket协议,无需ws://前缀
VITE_WS_URL=http://localhost:8000

# 开发环境
NODE_ENV=development
