# OpenAI 配置
OPENAI_API_KEY=auth-bowong777

# JWT 认证密钥
JWT_SECRET=sker-development-jwt-secret-key-2024

# 数据库配置 (生产环境)
DATABASE_URL=postgresql://sker_user:sker_password@postgres:5432/sker_db
REDIS_URL=redis://redis:6379
REDIS_PASSWORD=
RABBITMQ_URL=amqp://sker_user:sker_password@rabbitmq:5672

# 开发环境数据库配置
DEV_DATABASE_URL=postgresql://sker_dev_user:sker_dev_password@localhost:5432/sker_dev_db
DEV_REDIS_URL=redis://localhost:6379
DEV_RABBITMQ_URL=amqp://sker_dev_user:sker_dev_password@localhost:5672

# Node 运行环境
NODE_ENV=development

# 服务端口配置
GATEWAY_PORT=8000
ENGINE_PORT=8001
STORE_PORT=3001
BROKER_PORT=3002

# Redis 数据库编号配置
REDIS_DB=0

# 缓存配置
CACHE_TTL=3600              # 缓存生存时间(秒)
CACHE_MAX_KEYS=10000        # 最大缓存键数量

# OpenAI 服务提供商配置
USE_CUSTOM_PROVIDER=true                        # 是否使用自定义提供商
OPENAI_BASE_URL=https://gateway.bowong.cc       # OpenAI API 基础URL
OPENAI_DEFAULT_MODEL=gpt-4                      # 默认使用的AI模型
OPENAI_TIMEOUT=30000                            # API 请求超时时间(毫秒)
OPENAI_MAX_RETRIES=3                            # 最大重试次数

# 跨域资源共享(CORS)配置
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080
STUDIO_CORS_ORIGIN=http://localhost:3000,http://localhost:5173,http://localhost:8080

# 限流配置
RATE_LIMIT_WINDOW=900000        # 限流时间窗口(毫秒) 15分钟
RATE_LIMIT_MAX=100              # 时间窗口内最大请求数
API_RATE_LIMIT_WINDOW=900000    # API 限流时间窗口(毫秒)
API_RATE_LIMIT_MAX=100          # API 时间窗口内最大请求数
API_TIMEOUT=120000              # API 超时时间(毫秒)

# PostgreSQL 数据库详细配置
PG_HOST=postgres        # 数据库主机
PG_PORT=5432           # 数据库端口
PG_DATABASE=sker_db    # 数据库名称
PG_USER=sker_user      # 数据库用户名
PG_PASSWORD=sker_password  # 数据库密码
PG_SSL=false           # 是否使用SSL连接

# 微服务间通信配置
STORE_SERVICE_URL=http://store:3001      # 存储服务URL
BROKER_SERVICE_URL=http://broker:3002    # 消息代理服务URL
ENGINE_SERVICE_URL=http://engine:8001    # AI引擎服务URL

# AI引擎服务配置
STUDIO_API_PORT=8001     # Studio API 端口
STUDIO_API_HOST=0.0.0.0  # Studio API 主机地址

# 健康检查配置
ENABLE_HTTP_HEALTH_CHECK=true  # 启用HTTP健康检查
HEALTH_CHECK_PORT=3002         # 健康检查端口

# 前端API配置
API_BASE_URL=http://gateway:8000    # API 基础URL
WS_BASE_URL=ws://gateway:8000       # WebSocket 基础URL